name: Docker Image CI

on:
  push:
    tags: '*'

jobs:

  build:
    runs-on: srv
    steps:
    - uses: actions/checkout@v4
    - name: Build the Docker image
      run: docker build . --file Dockerfile --tag sfapp:$(date +%s)
